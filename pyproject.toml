[project]
name = "mooting"
version = "0.1.2"
description = "A council where agent CLIs from different vendors deliberate, and a human decides."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "jyunming" }]
keywords = ["ai", "agents", "cli", "mcp", "multi-agent", "claude-code", "codex"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Topic :: Software Development",
]
# Not optional: an agent reaches the board through MCP, and the session is the
# way a person uses this. Both were extras while being load-bearing, so a clean
# install produced a tool that could not convene anything.
dependencies = [
    "mcp>=1.2",
    "prompt_toolkit>=3.0",
    "textual>=1.0",
]

[project.optional-dependencies]
# The council does not need a web server; only `mooting serve` does, so the
# core install stays as small as it was.
serve = ["aiohttp>=3.9"]
telegram = ["aiogram>=3"]
web = ["textual-serve>=1"]
# Word and PowerPoint files are zip archives of XML and need nothing. A PDF
# needs a parser, and a board that never sees one should not carry it.
pdf = ["pypdf>=4"]
dev = ["pytest>=8", "pytest-asyncio>=0.23", "aiohttp>=3.9", "aiogram>=3",
       "textual-serve>=1", "pypdf>=4"]

[project.urls]
Homepage = "https://jyunming.github.io/mooting/"
Documentation = "https://jyunming.github.io/mooting/docs/"
Source = "https://github.com/jyunming/mooting"
Issues = "https://github.com/jyunming/mooting/issues"

[project.scripts]
mooting = "mooting.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["mooting*"]

[tool.setuptools.package-data]
mooting = ["schema.sql"]

[tool.pytest.ini_options]
testpaths = ["tests"]
